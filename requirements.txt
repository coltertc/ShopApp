# Postgres (Supabase) — required when DATABASE_URL is set for jobs/run_inference.py
psycopg[binary]>=3.1.0

# Optional: only needed after Part 2 saves artifacts/fraud_model.joblib
joblib>=1.4.0
scikit-learn>=1.3.0
